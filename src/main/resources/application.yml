spring:
    application:
        name: rest-with-spring-boot-and-java-wagner
    
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/dbmysql?useTimezone=true&serverTimezone=UTC 

        username: wagner
        password: 123
        
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
        show-sql: false
